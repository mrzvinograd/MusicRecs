Configuration files can live here later if you want to move hyperparameters out of Python scripts.

Right now shared paths and a few common constants are centralized in [config.py](/D:/ZZZFORALL/zsoundproject/config.py).
